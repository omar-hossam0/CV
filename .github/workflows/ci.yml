name: CV Matching Platform - CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: hello omar 
        run: echo "hello omar this is the first build in the ci pipeline "  
    
  
  