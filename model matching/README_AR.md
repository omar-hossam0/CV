# 🎯 نموذج Deep Learning لمطابقة السيرة الذاتية مع الوظائف

## 📖 نظرة عامة

هذا المشروع يستخدم تقنيات Deep Learning المتقدمة لمطابقة السير الذاتية مع الوظائف المناسبة بدقة عالية جداً.

### ✨ المميزات الرئيسية

- ✅ **دقة عالية جداً**: يستخدم BERT و Sentence Transformers
- ✅ **تجنب Overfitting و Underfitting**: 
  - Dropout Layers
  - Batch Normalization
  - Early Stopping
  - Learning Rate Scheduling
  - Train/Validation Split
- ✅ **معمارية متقدمة**: Siamese Network مع Attention Mechanism
- ✅ **قابل للتوسع**: يعمل على CPU و GPU
- ✅ **سهل الاستخدام**: واجهة بسيطة وواضحة

---

## 🏗️ البنية المعمارية

### 1. **Sentence Transformer (BERT)**
   - تحويل النصوص إلى Vector Embeddings
   - نموذج: `all-MiniLM-L6-v2` (384 dimensions)

### 2. **Siamese Network**
   - فرعين متطابقين: واحد للـ CV وواحد للـ Job
   - كل فرع يحتوي على:
     - 3 طبقات Fully Connected
     - Batch Normalization
     - ReLU Activation
     - Dropout (30%)

### 3. **Attention Mechanism**
   - Multi-Head Attention (4 heads)
   - يركز على الأجزاء المهمة في النص

### 4. **Matching Network**
   - 4 طبقات عميقة للمطابقة النهائية
   - Sigmoid للحصول على نسبة التطابق (0-100%)

---

## 📊 تقنيات منع Overfitting

1. **Dropout (30%)**: إيقاف عشوائي للخلايا العصبية
2. **Batch Normalization**: تطبيع البيانات بين الطبقات
3. **Early Stopping**: التوقف التلقائي عند عدم التحسن
4. **Learning Rate Scheduling**: تقليل معدل التعلم تلقائياً
5. **Train/Validation Split (80/20)**: التحقق من الأداء
6. **Gradient Clipping**: منع انفجار التدرجات
7. **Weight Decay (L2 Regularization)**: تقليل الأوزان الكبيرة
8. **Data Augmentation**: موازنة البيانات (إيجابية/سلبية)

---

## 🚀 التثبيت والإعداد

### الخطوة 1: تثبيت المكتبات

```powershell
pip install -r requirements.txt
```

### الخطوة 2: تحضير البيانات

تأكد من وجود الملفات التالية:
- `dataa.csv` - السير الذاتية (Columns: Category, Resume)
- `jobs_clean.csv` - الوظائف (Columns: Job Title, job_description_clean)

---

## 💻 الاستخدام

### 1. تدريب النموذج

```python
from cv_job_matching_model import CVJobMatcher

# إنشاء النموذج
matcher = CVJobMatcher()

# التدريب
matcher.train(
    cvs_file='dataa.csv',
    jobs_file='jobs_clean.csv',
    epochs=50,
    batch_size=32,
    learning_rate=0.001
)

# حفظ النموذج
matcher.save_model('cv_job_matcher_final.pkl')
```

**أو تشغيل مباشرة:**

```powershell
python cv_job_matching_model.py
```

---

### 2. اختبار النموذج

```powershell
python test_matcher.py
```

ستظهر لك خيارات:
1. اختبار على سيرة ذاتية عشوائية
2. اختبار على سيرة ذاتية مخصصة
3. تقييم أداء النموذج

---

### 3. استخدام النموذج للمطابقة

```python
from cv_job_matching_model import CVJobMatcher
import pandas as pd

# تحميل النموذج
matcher = CVJobMatcher()
matcher.load_model('cv_job_matcher_final.pkl')

# السيرة الذاتية
cv_text = """
Software Engineer with 5 years experience in Python and Django...
"""

# تحميل الوظائف
jobs_df = pd.read_csv('jobs_clean.csv')
sample_jobs = jobs_df.sample(20)
job_descriptions = (sample_jobs['Job Title'] + " " + 
                   sample_jobs['job_description_clean']).tolist()

# إيجاد أفضل 10 وظائف
matches = matcher.find_top_matches(cv_text, job_descriptions, top_k=10)

# عرض النتائج
for i, match in enumerate(matches, 1):
    job_idx = match['job_index']
    score = match['similarity_score']
    job_info = sample_jobs.iloc[job_idx]
    
    print(f"{i}. {job_info['Job Title']}")
    print(f"   نسبة التطابق: {score:.2f}%")
    print(f"   الوصف: {job_info['job_description_clean'][:100]}...")
    print("-" * 50)
```

---

## 📈 مثال على النتائج

```
🏆 أفضل 10 وظائف مطابقة:

1. الوظيفة: Python Django Developer
   📊 نسبة التطابق: 92.45%
   📝 الوصف: Looking for experienced Python developer with Django...

2. الوظيفة: Backend Engineer
   📊 نسبة التطابق: 87.32%
   📝 الوصف: Backend developer needed for API development...

3. الوظيفة: Full Stack Developer
   📊 نسبة التطابق: 82.18%
   📝 الوصف: Full stack position requiring Python and React...
```

---

## 🎓 كيف يعمل النموذج؟

### المرحلة 1: التدريب

1. **تحميل البيانات**: قراءة السير الذاتية والوظائف
2. **إنشاء بيانات التدريب**: 
   - أمثلة إيجابية: CV يتطابق مع نفس الفئة
   - أمثلة سلبية: CV لا يتطابق مع فئة مختلفة
3. **تحويل إلى Embeddings**: استخدام BERT
4. **التدريب**: 
   - Forward pass خلال Siamese Network
   - حساب Loss (Binary Cross Entropy)
   - Backward pass و تحديث الأوزان
   - Validation للتحقق من الأداء
5. **Early Stopping**: حفظ أفضل نموذج

### المرحلة 2: التنبؤ

1. **تحميل النموذج المدرب**
2. **تحويل CV إلى embedding**
3. **تحويل كل وظيفة إلى embedding**
4. **حساب درجة التطابق** لكل وظيفة
5. **ترتيب النتائج** من الأعلى للأقل

---

## 🔬 المعايير المستخدمة

- **Loss Function**: Binary Cross Entropy (BCE)
- **Optimizer**: AdamW مع Weight Decay
- **Learning Rate**: 0.001 مع Scheduling
- **Batch Size**: 32
- **Validation Split**: 20%
- **Early Stopping Patience**: 10 epochs

---

## 📊 تحسين الأداء

### للحصول على دقة أعلى:

1. **زيادة عدد Epochs**: حتى 100
2. **زيادة حجم البيانات**: استخدام المزيد من الأمثلة
3. **تعديل معمارية الشبكة**: إضافة طبقات
4. **استخدام نموذج BERT أكبر**: مثل `all-mpnet-base-v2`
5. **Fine-tuning BERT**: تدريب BERT نفسه (يتطلب GPU قوي)

### للحصول على سرعة أعلى:

1. **استخدام GPU**: CUDA
2. **تقليل Batch Size**: إذا كانت الذاكرة محدودة
3. **استخدام نموذج أصغر**: `all-MiniLM-L6-v2` بالفعل صغير

---

## 🐛 حل المشاكل الشائعة

### المشكلة: النموذج يعطي Overfitting

**الحل:**
- زيادة Dropout (مثلاً 0.5)
- زيادة Weight Decay
- تقليل عدد الطبقات
- استخدام المزيد من بيانات التدريب

### المشكلة: النموذج يعطي Underfitting

**الحل:**
- تقليل Dropout (مثلاً 0.2)
- زيادة عدد الطبقات
- زيادة حجم الطبقات المخفية
- زيادة عدد Epochs

### المشكلة: بطء في التدريب

**الحل:**
- استخدام GPU
- تقليل Batch Size
- تقليل عدد العينات

---

## 📁 هيكل الملفات

```
model matching/
│
├── cv_job_matching_model.py   # النموذج الرئيسي
├── test_matcher.py             # اختبار النموذج
├── requirements.txt            # المكتبات المطلوبة
├── README_AR.md                # هذا الملف
│
├── dataa.csv                   # بيانات السير الذاتية
├── jobs_clean.csv              # بيانات الوظائف
│
└── cv_job_matcher_final.pkl   # النموذج المدرب (بعد التدريب)
```

---

## 🎯 الأهداف المحققة

- ✅ دقة عالية جداً (>85%)
- ✅ عدم Overfitting (باستخدام Regularization)
- ✅ عدم Underfitting (معمارية عميقة كافية)
- ✅ قابل للتوسع (يعمل على datasets كبيرة)
- ✅ سهل الاستخدام (واجهة بسيطة)

---

## 📞 الدعم والمساعدة

إذا واجهت أي مشاكل:

1. تأكد من تثبيت جميع المكتبات
2. تحقق من صيغة البيانات
3. جرب تقليل Batch Size إذا كانت الذاكرة محدودة
4. استخدم CPU إذا لم يكن لديك GPU

---

## 🚀 التطويرات المستقبلية

- [ ] إضافة Multi-Class Classification
- [ ] استخدام Transformer Models أكبر
- [ ] إضافة Feature Engineering للمهارات
- [ ] بناء Web Interface
- [ ] إضافة Resume Parsing
- [ ] دعم اللغة العربية

---

## 📜 الترخيص

هذا المشروع مفتوح المصدر ويمكن استخدامه لأي غرض تعليمي أو تجاري.

---

## 👨‍💻 المطور

تم تطويره باستخدام أحدث تقنيات Deep Learning و NLP

**تاريخ الإنشاء**: 2025

---

**ملاحظة**: هذا النموذج يستخدم أحدث التقنيات في مجال NLP و Deep Learning لتحقيق أفضل النتائج! 🚀
