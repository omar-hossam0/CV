# 📚 دليل المبتدئين - خطوة بخطوة

## 🎯 الهدف
بناء نموذج Deep Learning يطابق السيرة الذاتية مع أفضل الوظائف المناسبة.

---

## 📋 المتطلبات الأساسية

### 1. تثبيت Python

- نزل Python 3.8 أو أحدث من [python.org](https://www.python.org/downloads/)
- أثناء التثبيت، ✅ اختر "Add Python to PATH"

### 2. التحقق من التثبيت

افتح PowerShell وجرب:

```powershell
python --version
```

يجب أن ترى: `Python 3.8.x` أو أحدث

---

## 🚀 الخطوات

### الخطوة 1: فتح مجلد المشروع

```powershell
cd "d:\Dulms\Level3 term(1)\Project\code\model matching"
```

### الخطوة 2: تثبيت المكتبات المطلوبة

```powershell
pip install -r requirements.txt
```

⏱️ **ملاحظة**: قد يستغرق 5-10 دقائق

#### إذا واجهت مشكلة في التثبيت:

```powershell
# طريقة 1: تحديث pip أولاً
python -m pip install --upgrade pip
pip install -r requirements.txt

# طريقة 2: تثبيت واحدة تلو الأخرى
pip install torch
pip install sentence-transformers
pip install pandas numpy scikit-learn
pip install matplotlib seaborn
```

### الخطوة 3: التحقق من البيانات

تأكد من وجود الملفات:
- ✅ `dataa.csv` - السير الذاتية
- ✅ `jobs_clean.csv` - الوظائف

```powershell
# عرض أول 5 أسطر من البيانات
Get-Content dataa.csv -Head 5
Get-Content jobs_clean.csv -Head 5
```

### الخطوة 4: تدريب النموذج

#### الطريقة السهلة (باستخدام السكريبت):

```powershell
.\run.ps1
```

اختر الخيار `2` للتدريب أو `4` للتشغيل الكامل.

#### الطريقة اليدوية:

```powershell
python cv_job_matching_model.py
```

⏱️ **الوقت المتوقع**:
- مع CPU: 15-30 دقيقة
- مع GPU: 5-10 دقائق

#### ماذا سيحدث؟

سترى شيئاً مثل:

```
🚀 جاري تحميل نموذج BERT...
✅ استخدام: cpu

📂 جاري تحميل البيانات...
✅ تم تحميل 2484 سيرة ذاتية
✅ تم تحميل 2279 وظيفة

📊 جاري إنشاء بيانات التدريب...
✅ تم إنشاء 10000 عينة تدريب

🔄 جاري تحويل النصوص إلى embeddings...
100%|████████████████| 312/312 [02:15<00:00]

🚀 بدء التدريب...
Epoch [1/50]
  Train Loss: 0.6234 | Train Acc: 65.32%
  Val Loss: 0.5876 | Val Acc: 68.45%
...
```

### الخطوة 5: اختبار النموذج

بعد انتهاء التدريب:

```powershell
python test_matcher.py
```

اختر من القائمة:
1. **اختبار على سيرة ذاتية عشوائية** - للتجربة
2. **اختبار على سيرة ذاتية مخصصة** - أدخل سيرتك
3. **تقييم أداء النموذج** - اختبار شامل

### الخطوة 6: استخدام النموذج

أنشئ ملف جديد `my_test.py`:

```python
from cv_job_matching_model import CVJobMatcher
import pandas as pd

# 1. تحميل النموذج
matcher = CVJobMatcher()
matcher.load_model('cv_job_matcher_final.pkl')

# 2. سيرتك الذاتية
my_cv = """
أنا مهندس برمجيات خبرة 3 سنوات.
أتقن Python, Django, Machine Learning
خبرة في تطوير APIs و Web Applications
"""

# 3. تحميل الوظائف
jobs = pd.read_csv('jobs_clean.csv')
sample = jobs.sample(20)  # اختر 20 وظيفة عشوائياً

# تحضير النصوص
job_texts = (sample['Job Title'] + " " + 
             sample['job_description_clean']).tolist()

# 4. إيجاد المطابقات
matches = matcher.find_top_matches(my_cv, job_texts, top_k=10)

# 5. عرض النتائج
print("\n🏆 أفضل 10 وظائف لك:\n")
for i, match in enumerate(matches, 1):
    idx = match['job_index']
    score = match['similarity_score']
    job = sample.iloc[idx]
    
    print(f"{i}. {job['Job Title']}")
    print(f"   📊 نسبة التطابق: {score:.2f}%")
    print(f"   📝 الوصف: {job['job_description_clean'][:100]}...")
    print("-" * 60)
```

شغل الملف:

```powershell
python my_test.py
```

---

## 🎨 الرسوم البيانية

لعرض الإحصائيات والرسوم البيانية:

```powershell
python visualize_results.py
```

اختر:
1. **تقرير شامل** - كل الرسوم
2. **منحنيات التدريب** - Loss و Accuracy
3. **توزيع النتائج** - إحصائيات المطابقة
4. **أداء الفئات** - أداء كل نوع وظيفة

---

## 🔧 تخصيص الإعدادات

### لتغيير عدد الـ Epochs:

افتح `config.py` وعدل:

```python
TRAINING_CONFIG = {
    'epochs': 30,  # كان 50، غيره لـ 30 للسرعة
    ...
}
```

### لاستخدام إعدادات جاهزة:

```python
from config import apply_preset

# للتدريب السريع
apply_preset('fast')

# للدقة العالية
apply_preset('high_accuracy')

# المتوازن (الافتراضي)
apply_preset('balanced')
```

---

## 📊 فهم النتائج

### نسب التطابق:

- **90-100%**: مطابقة ممتازة ⭐⭐⭐
- **80-89%**: مطابقة جيدة جداً ⭐⭐
- **70-79%**: مطابقة جيدة ⭐
- **أقل من 70%**: مطابقة ضعيفة

### مثال على النتيجة:

```
🏆 أفضل 10 وظائف:

1. Python Django Developer
   📊 نسبة التطابق: 92.45%
   📝 الوصف: Looking for Python developer with Django...
   
2. Backend Engineer
   📊 نسبة التطابق: 87.32%
   📝 الوصف: Backend position requiring Python skills...
```

---

## ❓ الأسئلة الشائعة

### س1: النموذج بطيء جداً!

**ج:** جرب:
```python
# في cv_job_matching_model.py
matcher.train(
    ...,
    epochs=20,      # بدلاً من 50
    batch_size=64,  # بدلاً من 32
)
```

### س2: خطأ Out of Memory

**ج:** قلل Batch Size:
```python
matcher.train(..., batch_size=16)  # أو حتى 8
```

### س3: الدقة منخفضة

**ج:** زود عدد الـ Epochs:
```python
matcher.train(..., epochs=100)
```

### س4: كيف أستخدم GPU؟

**ج:** النموذج يكتشف GPU تلقائياً. للتأكد:
```python
import torch
print(torch.cuda.is_available())  # يجب أن يكون True
```

### س5: النتائج عشوائية!

**ج:** تأكد من:
1. تدريب النموذج كاملاً (لا توقفه مبكراً)
2. استخدام نفس النموذج المحفوظ
3. جودة البيانات المدخلة

---

## 🎯 تمارين للتطبيق

### تمرين 1: اختبر سيرتك الذاتية

1. اكتب سيرتك الذاتية بالإنجليزية
2. شغل النموذج عليها
3. شوف أفضل 10 وظائف لك

### تمرين 2: قارن بين سيرتين

1. جرب سيرة ذاتية لـ Python Developer
2. جرب سيرة ذاتية لـ Java Developer
3. قارن النتائج

### تمرين 3: حسّن النموذج

1. غير الإعدادات في `config.py`
2. درب النموذج مرة ثانية
3. قارن الدقة قبل وبعد

---

## 📞 المساعدة

إذا واجهتك مشكلة:

1. ✅ تأكد من تثبيت كل المكتبات
2. ✅ تأكد من وجود ملفات البيانات
3. ✅ جرب تشغيل الأمثلة البسيطة أولاً
4. ✅ راجع رسائل الأخطاء بعناية

---

## 🎓 تعلم أكثر

### فهم Deep Learning:
- [PyTorch Tutorial](https://pytorch.org/tutorials/)
- [Deep Learning Book](http://www.deeplearningbook.org/)

### فهم NLP و BERT:
- [BERT Paper](https://arxiv.org/abs/1810.04805)
- [Sentence Transformers](https://www.sbert.net/)

### فهم Siamese Networks:
- [One-shot Learning Paper](https://www.cs.cmu.edu/~rsalakhu/papers/oneshot1.pdf)

---

## ✅ Checklist النجاح

- [ ] تثبيت Python بنجاح
- [ ] تثبيت كل المكتبات
- [ ] تدريب النموذج كاملاً
- [ ] اختبار النموذج
- [ ] الحصول على دقة >80%
- [ ] اختبار سيرتك الذاتية
- [ ] فهم كيف يعمل النموذج

---

**🎉 مبروك! أنت الآن قادر على بناء نموذج AI لمطابقة السير الذاتية!**

**💡 النصيحة الذهبية**: لا تستعجل، افهم كل خطوة، وجرب بنفسك!
